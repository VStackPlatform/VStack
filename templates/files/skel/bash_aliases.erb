#
# THIS FILE IS MANAGED BY THE VAGRANT STACK.
# /vagrant/files/skel/bash_aliases
#

if [ -f /etc/bash_completion ]; then
    source /etc/bash_completion
fi

black=$(tput -Txterm setaf 0)
red=$(tput -Txterm setaf 1)
green=$(tput -Txterm setaf 2)
yellow=$(tput -Txterm setaf 3)
dk_blue=$(tput -Txterm setaf 4)
pink=$(tput -Txterm setaf 5)
lt_blue=$(tput -Txterm setaf 6)

bold=$(tput -Txterm bold)
reset=$(tput -Txterm sgr0)

GIT_PS1_SHOWDIRTYSTATE=true

# Nicely formatted terminal prompt
export PS1='\n\[$bold\]\[$black\][\[$dk_blue\]\@\[$black\]]-[\[$green\]\u\[$yellow\]@\[$green\]\h\[$black\]]-[\[$pink\]\w\[$black\]]\[\033[0;33m\]$(__git_ps1) \[\033[00m\]\[$reset\]\n\[$reset\]\[$dk_blue\](<%= @projectName %>)\[$reset\]\$ '

# Handy Aliases
alias ls='ls -F --color=always'
alias dir='dir -F --color=always'
alias ll='ls -l'
alias cp='cp -iv'
alias rm='rm -i'
alias mv='mv -iv'
alias grep='grep --color=auto -in'
alias ..='cd ..'
alias cl='clear'
alias egrep='egrep --color=auto'
alias fgrep='fgrep --color=auto'
alias l='ls --color -l'
alias la='ls --color -la'
alias lh='ls --color -lh'

<% if @nodejs %>
# NVM for Nodejs management.
export NVM_DIR="/usr/local/nvm"
[ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh"  # This loads nvm
<% end %>
