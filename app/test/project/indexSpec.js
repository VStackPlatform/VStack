define(['project/index'], function(project) {
    describe("test that project page works correctly.", function() {
        it("loads all addons.", function() {
            /*project.activate();
            project.resetProject();*/
        });
    });
});